serviceType : LoadBalancer
externalTrafficPolicy: Local
loadBalancerIP: 10.0.7.80
replicas: 3
memoryLimit: 100Mi
cpuLimit: 1
debug:
  enabled: false
rbac:
  enabled: true
ssl:
  enabled: true
  enforced: true
  insecureSkipVerify : true
kvprovider:
  storeAcme: true
  consul:
    endpoint: consul:8500
    watch: true
    prefix: "\"traefik\""
acme:
  enabled: true
  email: $EMAIL
  staging: false
  logging: true
  domains:
    enabled: true
    domainsList:
      - main: "*.$DOMAIN"
      - sans:
        - "$DOMAIN"
      - main: "*.k.$DOMAIN"
      - main: "*.nd.$DOMAIN"
      - main: "*.ha.$DOMAIN"
  challengeType: dns-01
  dnsProvider:
    name: digitalocean
    digitalocean:
      DO_AUTH_TOKEN: "$DO_AUTH_TOKEN"
  persistence:
    enabled: false
dashboard:
  enabled: true
  domain: t.k.$DOMAIN
  auth :
    basic:
       $TRAEFIK_AUTH
  statistics:
    recentErrors: 25
deployment:
  hostPort:
    dashboardEnabled: true
keel:
  policy: all
  trigger: poll
  pollSchedule: "@every 1h"
  approvals: 1
  images:
    - repository: image
      tag: imageTag
