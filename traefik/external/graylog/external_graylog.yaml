apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: graylog
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: gl.$DOMAIN
    http:
      paths:
        - backend:
            serviceName: graylog
            servicePort: 9000
---
apiVersion: v1
kind: Service
metadata:
  name: graylog
spec:
  ports:
  - name: graylog
    port: 9000
  type: ExternalName
  externalName: swarm